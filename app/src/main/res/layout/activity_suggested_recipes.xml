<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#F7F9F7"
    tools:context=".SuggestedRecipesActivity">

    <TextView
        android:id="@+id/tvRecipesTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Suggested Recipes"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="#1B5E20"
        android:gravity="center"
        android:paddingTop="16dp"
        android:paddingBottom="8dp" />

    <TextView
        android:id="@+id/tvRecipesSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Recipes you can make using ingredients in your pantry."
        android:textSize="14sp"
        android:textColor="#616161"
        android:gravity="center"
        android:paddingBottom="20dp" />

    <TextView
        android:id="@+id/tvNoRecipes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No recipes available yet."
        android:textSize="16sp"
        android:textColor="#757575"
        android:gravity="center"
        android:padding="24dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewRecipes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="8dp"
        tools:listitem="@android:layout/simple_list_item_2" />

    <Button
        android:id="@+id/btnBackToPantry"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back to Pantry"
        android:textAllCaps="false"
        android:layout_marginTop="8dp" />

</LinearLayout>